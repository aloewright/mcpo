{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "uv pip install mcpo && mcpo proxy --base-url 0.0.0.0:$PORT --forward-to https://mcp.composio.dev/ --auth x-api-key=X_API_KEY",
    "healthcheckPath": "/"
  }
}