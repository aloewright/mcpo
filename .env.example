# Environment Variables for Composio Proxy

# Railway will automatically provide this - DO NOT SET MANUALLY
PORT=8000

# Optional: Default Composio API key if not provided via Bearer token
# If set, this will be used when Open WebUI doesn't provide an Authorization header
# Otherwise, the Bearer token from Open WebUI will be used as the x-api-key for Composio
X_API_KEY=your_composio_api_key_here

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Configuration Notes:
# 
# 1. Open WebUI Setup:
#    - Set your proxy URL to: https://your-railway-app.railway.app
#    - Configure Bearer token in Open WebUI with your Composio API key
#    - The proxy will transform "Authorization: Bearer <key>" to "x-api-key: <key>"
#
# 2. Railway Setup:
#    - Add X_API_KEY as environment variable in Railway dashboard (optional)
#    - Set LOG_LEVEL=DEBUG for troubleshooting connection issues
#    - PORT is automatically provided by Railway
#
# 3. Authentication Flow:
#    - Open WebUI sends: Authorization: Bearer <composio_api_key>
#    - Proxy transforms to: x-api-key: <composio_api_key>
#    - Forwards request to: https://mcp.composio.dev/
